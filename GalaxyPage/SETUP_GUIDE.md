# Galaxy of Memories - Setup Guide

## 📋 Overview
Galaxy of Memories เป็น Single Page Application ที่ใช้ Three.js สร้างอวกาศ 3D และเชื่อมต่อกับ Google Sheets เป็นฐานข้อมูล

## 🚀 Setup Instructions

### 1. Setup Google Sheets

1. สร้าง Google Sheets ใหม่
2. เพิ่มคอลัมน์ต่อไปนี้ในแถวแรก:
   - `Date` - วันที่ความทรงจำ
   - `Title` - หัวข้อ
   - `Message` - ข้อความ
   - `Category` - หมวดหมู่ (Friendship, Adventure, Funny, Special, Random)
   - `ImageURL` - URL ของรูปภาพ
   - `CreatedAt` - วันเวลาที่สร้าง

3. เพิ่มข้อมูลตัวอย่าง:
   ```
   Date        | Title              | Message                    | Category   | ImageURL | CreatedAt
   2020-01-15  | วันที่เจอกันครั้งแรก | วันที่เราเริ่มต้นมิตรภาพ    | Friendship |          | 2020-01-15
   2020-06-20  | ผจญภัยครั้งแรก      | เราไปเที่ยวด้วยกัน           | Adventure  |          | 2020-06-20
   ```

### 2. Setup Google Drive Folder

1. สร้าง Folder ใน Google Drive สำหรับเก็บรูปภาพ
2. คัดลอก Folder ID จาก URL (ส่วนหลัง `/folders/`)
   - ตัวอย่าง: `https://drive.google.com/drive/folders/ABC123XYZ`
   - Folder ID = `ABC123XYZ`

### 3. Deploy Google Apps Script

1. เปิด Google Sheets ที่สร้างไว้
2. ไปที่ **Extensions > Apps Script**
3. ลบโค้ดเดิมทั้งหมด
4. คัดลอกโค้ดจากไฟล์ `google-apps-script.gs` ทั้งหมด
5. แก้ไข `FOLDER_ID` ให้ตรงกับ Folder ID ของคุณ:
   ```javascript
   const FOLDER_ID = 'ABC123XYZ'; // ← ใส่ Folder ID ของคุณ
   ```
6. **บันทึก** โปรเจค (Ctrl+S)
7. คลิก **Deploy > New deployment**
8. คลิก **Select type** เลือก **Web app**
9. ตั้งค่า:
   - Description: `Galaxy of Memories API`
   - Execute as: **Me**
   - Who has access: **Anyone**
10. คลิก **Deploy**
11. **คัดลอก Web App URL** (จะมีหน้าตาแบบนี้):
    ```
    https://script.google.com/macros/s/ABC.../exec
    ```

### 4. Configure Frontend

1. เปิดไฟล์ `galaxy-script.js`
2. แทนที่ `API_URL` ด้วย Web App URL ที่คัดลอกมา:
   ```javascript
   const API_URL = 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec';
   ```
3. บันทึกไฟล์

### 5. Test Locally

1. เปิดไฟล์ `galaxy.html` ในเบราว์เซอร์
2. รอให้โหลดดวงดาวจาก Google Sheets
3. ทดสอบคลิกดวงดาวเพื่อดูความทรงจำ
4. ทดสอบกด "Launch New Memory" เพื่อเพิ่มข้อมูลใหม่

## 🎨 Features

### ✨ Three.js Features
- **3D Space Scene** - อวกาศ 3D พร้อม nebula effect
- **Dynamic Stars** - ดวงดาวถูกสร้างจากข้อมูลใน Google Sheets
- **Constellation Lines** - เส้นเชื่อมดวงดาวตาม category
- **Parallax Effect** - กล้องหมุนช้าๆ แบบ parallax

### 🎯 Interactive Features
- **Click to Zoom** - คลิกดวงดาวเพื่อ zoom เข้าด้วย GSAP
- **Glassmorphism Modal** - แสดงความทรงจำแบบ glassmorphism
- **Launch System** - ส่งความทรงจำใหม่พร้อมแอนิเมชันยานอวกาศ
- **Image Upload** - อัปโหลดรูปภาพเป็น Base64 และเก็บใน Google Drive

### 🎨 Category Colors
- 💗 **Friendship** - Pink (#ff6b9d)
- ⭐ **Adventure** - Gold (#ffd700)
- 💚 **Funny** - Green (#00ff88)
- 💙 **Special** - Light Blue (#a8daff)
- 🧡 **Random** - Orange (#ff9500)

## 📱 Deployment to GitHub Pages

1. Push โปรเจคขึ้น GitHub
2. ไปที่ Settings > Pages
3. เลือก Branch: main
4. เลือก Folder: / (root)
5. Save
6. รอสักครู่แล้วเข้า `https://username.github.io/repository-name/GalaxyPage/galaxy.html`

## 🔒 Security Notes

- Web App URL เป็น public แต่ Google จัดการ authentication
- รูปภาพใน Google Drive จะถูกตั้งค่าเป็น "Anyone with link can view"
- ไม่ควรเก็บข้อมูลที่เป็นความลับใน Google Sheets นี้

## 🐛 Troubleshooting

### ดวงดาวไม่ปรากฏ
- ตรวจสอบว่า API_URL ถูกต้อง
- เปิด Console (F12) ดู error messages
- ตรวจสอบว่า Apps Script deploy เป็น "Anyone" access

### Upload รูปไม่ได้
- ตรวจสอบว่า FOLDER_ID ถูกต้อง
- ตรวจสอบขนาดไฟล์ (ไม่เกิน 5MB)
- ตรวจสอบว่า Apps Script มีสิทธิ์เข้าถึง Google Drive

### Background music ไม่เล่น
- Browser บางตัว block autoplay
- ลองคลิกหน้าจอก่อนเพื่อ trigger การเล่น

## 📚 Libraries Used

- **Three.js** r128 - 3D Graphics
- **GSAP** 3.12.2 - Animations
- **Fetch API** - HTTP Requests

## 🎉 Enjoy Your Galaxy!

ตอนนี้คุณมีจักรวาลแห่งความทรงจำของคุณเองแล้ว! ✨🌌
